// DESCRIPTION:
// Эта функция должна проверять, является ли фактор .factorbase

// Вернитесь, если это фактор или если это не так.truefalse

function checkForFactor (base, factor) {
  return base%factor===0
}
